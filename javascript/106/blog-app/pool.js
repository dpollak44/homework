/*const mysql = require('mysql');

const pool = mysql.createPool({
    connectionLimit: 10,
    host: 'localhost',
    user: 'dpollak',
    password: 'password',
    database: 'pcs'
});

module.exports = pool;*/